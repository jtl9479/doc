# 50장: 도서관 관리 시스템 (종합 프로젝트)

> **학습 목표**: 여러 시스템을 통합하여 실무 수준의 도서관 관리 시스템을 완성하고, 객체지향 설계 원칙을 적용할 수 있습니다.

**⏱️ 예상 학습 시간**: 10-12시간
**난이도**: ⭐⭐⭐⭐⭐ (5/5)

---

## 📚 목차
- [왜 이 기술이 필요한가](#왜-이-기술이-필요한가)
- [실생활 비유로 이해하기](#실생활-비유로-이해하기)
- [핵심 개념](#핵심-개념)
- [기본 실습](#기본-실습)
- [실무 활용 사례](#실무-활용-사례)
- [주니어 시나리오](#주니어-시나리오)
- [실전 프로젝트](#실전-프로젝트)
- [FAQ](#faq)
- [면접 질문 리스트](#면접-질문-리스트)
- [핵심 정리](#핵심-정리)
- [다음 단계](#다음-단계)

---

## 🤔 왜 이 기술이 필요한가?

### 실무 배경

**도서관 관리 시스템은 여러 도메인(도서, 회원, 대출)이 복잡하게 얽힌 종합 시스템입니다. 이를 통해 실무 수준의 설계 능력을 배양할 수 있습니다.**

#### ❌ 체계적인 시스템 설계를 모르면 발생하는 문제

```
문제 1: 데이터 정합성 문제
- 증상: 대출 중인 책이 중복 대출됨
- 영향: 도서 분실, 회원 불만
- 비용: 도서 재구매 비용 연간 $5,000

문제 2: 연체 관리 미흡
- 증상: 연체료 계산 오류, 연체자 추적 불가
- 영향: 수익 손실, 도서 회전율 저하
- 비용: 연간 수익 손실 $10,000

문제 3: 통계 및 보고 기능 부재
- 증상: 인기 도서, 대출 통계 파악 불가
- 영향: 데이터 기반 의사결정 불가
- 비용: 비효율적 예산 집행 연간 $20,000

문제 4: 스파게티 코드
- 증상: 모든 로직이 한 파일에 얽혀있음
- 영향: 유지보수 불가능, 버그 수정 어려움
- 비용: 개발 생산성 70% 저하
```

#### ✅ 체계적인 도서관 시스템을 구축하면

```
해결책 1: 도메인 주도 설계(DDD)
- 방법: Book, Member, Loan 도메인 분리
- 효과: 책임 명확화, 유지보수성 향상
- 절감: 개발 시간 50% 단축

해결책 2: 상태 패턴 적용
- 방법: 도서 상태(대출 가능, 대출 중, 연체)를 enum으로 관리
- 효과: 비즈니스 로직 명확화
- 절감: 버그 발생률 80% 감소

해결책 3: Repository 패턴
- 방법: 데이터 접근 계층 분리
- 효과: 테스트 용이성, 확장성 향상
- 절감: 테스트 시간 60% 단축

해결책 4: 자동 연체 관리
- 방법: 스케줄러로 매일 연체 체크
- 효과: 연체료 누락 0%
- 절감: 연간 수익 증가 $15,000
```

### 📊 수치로 보는 효과

| 지표 | 수작업 관리 | 시스템 관리 | 개선율 |
|------|------------|------------|--------|
| 도서 대출 처리 시간 | 5분 | 30초 | **90%↓** |
| 연체 확인 시간 | 2시간/일 | 1초 | **99.9%↓** |
| 중복 대출 발생률 | 5% | 0% | **100%↓** |
| 통계 보고서 작성 | 1일 | 1분 | **99.9%↓** |
| 코드 유지보수 시간 | 20시간/월 | 5시간/월 | **75%↓** |
| 도서 분실률 | 3% | 0.5% | **83%↓** |

### 🏢 실무 적용 사례

**카카오 - 전자책 관리 시스템**
```
도전 과제: 1,000만+ 사용자의 전자책 대출 관리
해결 방법:
- 도메인 주도 설계로 복잡도 관리
- 이벤트 소싱으로 대출 이력 추적
- Redis 캐싱으로 성능 최적화

결과:
✅ 동시 접속 10만 명 처리
✅ 응답 시간 100ms 이하
✅ 99.99% 가용성 달성
```

**네이버 시리즈 - 만화/소설 대여 시스템**
```
도전 과제: 복잡한 대여 정책 구현
해결 방법:
- 상태 패턴으로 대여 상태 관리
- 전략 패턴으로 가격 정책 분리
- 옵저버 패턴으로 만료 알림

결과:
✅ 정책 변경 시간 3일 → 1시간
✅ 자동 만료 처리 정확도 100%
✅ 고객 불만 50% 감소
```

---

## 🌟 실생활 비유로 이해하기

### 비유 1: 실제 도서관 시스템

```
물리적 도서관 = 프로그램 구조

1. 사서(Librarian) = LibraryService
   - 역할: 대출/반납 업무 총괄
   - 책임: 비즈니스 로직 처리
   → 중앙 제어 담당

2. 책장(Bookshelf) = BookRepository
   - 역할: 도서 보관 및 관리
   - 책임: 데이터 저장/조회
   → 데이터 계층 담당

3. 회원증(MemberCard) = Member 객체
   - 역할: 회원 정보 보관
   - 책임: 회원 상태 관리
   → 도메인 모델 담당

4. 대출증(LoanSlip) = Loan 객체
   - 역할: 대출 정보 기록
   - 책임: 대출 상태 추적
   → 관계 모델 담당

5. 도서 분류(Catalog) = Book 카테고리
   - 역할: 도서 분류 및 검색
   - 책임: 정보 조직화
   → 도메인 로직 담당
```

### 비유 2: 음식 배달 시스템

```
배달 앱 = 도서관 시스템

1. 고객 = Member
   - 음식 주문 = 도서 대출
   - 주문 내역 = 대출 이력
   → 사용자 도메인

2. 메뉴 = Book
   - 재고 상태 = 도서 상태
   - 카테고리 = 도서 분류
   → 상품 도메인

3. 주문 = Loan
   - 주문 상태(접수, 준비, 배달) = 대출 상태
   - 배달 완료 = 반납
   → 트랜잭션 도메인

4. 배달원 = System
   - 자동 배정 = 자동 연체 체크
   - 상태 업데이트 = 상태 전이
   → 시스템 로직

실무 적용:
배민 시스템에서 주문-상품-고객 관계는
도서관의 대출-도서-회원 관계와 동일한 구조
```

### 비유 3: 은행 계좌 시스템

```
은행 = 도서관

1. 계좌(Account) = Member
   - 계좌 잔액 = 대출 가능 권수
   - 계좌 상태(정상, 정지) = 회원 상태
   → 제약 조건 관리

2. 거래(Transaction) = Loan
   - 입금/출금 = 대출/반납
   - 거래 내역 = 대출 이력
   → 이력 관리

3. 자동 이체 = 자동 연체 체크
   - 예약 시간 = 매일 자정
   - 실행 조건 = 반납일 초과
   → 배치 작업

4. 이자 계산 = 연체료 계산
   - 복리 = 일수 계산
   - 자동 부과 = 자동 연체료
   → 규칙 엔진

실무 적용:
토스뱅크의 거래 시스템은
도서관 대출 시스템과 유사한 상태 관리 사용
```

### 비유 4: 렌터카 시스템

```
렌터카 = 도서관

1. 차량(Car) = Book
   - 대여 가능/불가 = 대출 가능/불가
   - 차량 상태(양호, 수리 중) = 도서 상태
   → 자원 관리

2. 대여(Rental) = Loan
   - 대여 기간 = 대출 기간
   - 연체료 = 도서 연체료
   → 시간 기반 로직

3. 반납 검사 = 도서 상태 확인
   - 손상 체크 = 파손 확인
   - 연료 체크 = 페이지 누락 확인
   → 검증 로직

4. 예약 시스템 = 예약 대기
   - 대기열 = 예약 큐
   - 자동 알림 = 반납 알림
   → 큐 관리

실무 적용:
쏘카의 차량 대여 시스템은
도서관과 동일한 예약-대출-반납 플로우 사용
```

### 비유 5: 호텔 예약 시스템

```
호텔 = 도서관

1. 객실(Room) = Book
   - 객실 상태(예약 가능, 투숙 중, 청소 중) = 도서 상태
   - 객실 타입(스탠다드, 디럭스) = 도서 분류
   → 재고 관리

2. 예약(Reservation) = Loan
   - 체크인/체크아웃 = 대출/반납
   - 투숙 기간 = 대출 기간
   → 기간 관리

3. 회원 등급 = 회원 등급
   - 일반/VIP = 일반/우수 회원
   - 등급별 혜택 = 대출 권수 차등
   → 등급 시스템

4. 늦은 체크아웃 = 연체
   - 추가 요금 = 연체료
   - 자동 정산 = 자동 계산
   → 요금 시스템

실무 적용:
야놀자의 예약 시스템은
도서관의 예약-대출 로직과 거의 동일
```

---

## 💡 핵심 개념

### 1단계: 도메인 모델 설계

**도메인 모델은 비즈니스 로직의 핵심입니다. 각 도메인의 책임을 명확히 분리해야 합니다.**

#### Book (도서 도메인)

```java
public class Book {
    private Long id;
    private String isbn;
    private String title;
    private String author;
    private String publisher;
    private BookCategory category;
    private BookStatus status;
    private LocalDate publishDate;

    // 비즈니스 로직
    public boolean isAvailable() {
        return status == BookStatus.AVAILABLE;
    }

    public void checkout() {
        if (!isAvailable()) {
            throw new IllegalStateException("이 도서는 대출할 수 없습니다.");
        }
        this.status = BookStatus.CHECKED_OUT;
    }

    public void returnBook() {
        this.status = BookStatus.AVAILABLE;
    }
}

public enum BookStatus {
    AVAILABLE("대출 가능"),
    CHECKED_OUT("대출 중"),
    RESERVED("예약됨"),
    LOST("분실"),
    DAMAGED("파손");

    private final String description;

    BookStatus(String description) {
        this.description = description;
    }
}

public enum BookCategory {
    FICTION("소설"),
    NON_FICTION("비문학"),
    SCIENCE("과학"),
    HISTORY("역사"),
    COMPUTER("컴퓨터"),
    BUSINESS("경영"),
    ARTS("예술");

    private final String description;

    BookCategory(String description) {
        this.description = description;
    }
}
```

**핵심 포인트**:
- ✅ 상태(enum)로 도서 상태 관리 → 타입 안전성
- ✅ 비즈니스 로직을 도메인 객체 내부에 → 응집도 향상
- ✅ 불변성 보장 (final 필드)

#### Member (회원 도메인)

```java
public class Member {
    private Long id;
    private String memberNumber;
    private String name;
    private String email;
    private String phone;
    private MemberGrade grade;
    private MemberStatus status;
    private LocalDate registeredDate;
    private int currentLoanCount;

    // 비즈니스 로직
    public boolean canBorrow() {
        return status == MemberStatus.ACTIVE
            && currentLoanCount < grade.getMaxLoanCount();
    }

    public void incrementLoanCount() {
        if (!canBorrow()) {
            throw new IllegalStateException("대출 한도를 초과했습니다.");
        }
        this.currentLoanCount++;
    }

    public void decrementLoanCount() {
        if (currentLoanCount > 0) {
            this.currentLoanCount--;
        }
    }

    public void suspend() {
        this.status = MemberStatus.SUSPENDED;
    }
}

public enum MemberGrade {
    BRONZE("브론즈", 3, 14),
    SILVER("실버", 5, 21),
    GOLD("골드", 7, 30),
    VIP("VIP", 10, 60);

    private final String description;
    private final int maxLoanCount;
    private final int maxLoanDays;

    MemberGrade(String description, int maxLoanCount, int maxLoanDays) {
        this.description = description;
        this.maxLoanCount = maxLoanCount;
        this.maxLoanDays = maxLoanDays;
    }

    public int getMaxLoanCount() { return maxLoanCount; }
    public int getMaxLoanDays() { return maxLoanDays; }
}

public enum MemberStatus {
    ACTIVE("활성"),
    SUSPENDED("정지"),
    WITHDRAWN("탈퇴");

    private final String description;

    MemberStatus(String description) {
        this.description = description;
    }
}
```

**핵심 포인트**:
- ✅ 회원 등급별 대출 권수/기간 차등 적용
- ✅ 비즈니스 규칙을 enum에 캡슐화
- ✅ 상태 전이 로직 포함

#### Loan (대출 도메인)

```java
public class Loan {
    private Long id;
    private Long bookId;
    private Long memberId;
    private LocalDate loanDate;
    private LocalDate dueDate;
    private LocalDate returnDate;
    private LoanStatus status;
    private int overdueFee;

    // 비즈니스 로직
    public boolean isOverdue() {
        if (returnDate != null) {
            return false; // 이미 반납됨
        }
        return LocalDate.now().isAfter(dueDate);
    }

    public int calculateOverdueDays() {
        if (!isOverdue()) {
            return 0;
        }
        return (int) ChronoUnit.DAYS.between(dueDate, LocalDate.now());
    }

    public int calculateOverdueFee() {
        int days = calculateOverdueDays();
        return days * 100; // 1일당 100원
    }

    public void returnBook() {
        this.returnDate = LocalDate.now();
        this.status = LoanStatus.RETURNED;
        this.overdueFee = calculateOverdueFee();
    }

    public void extend(int days) {
        if (isOverdue()) {
            throw new IllegalStateException("연체된 도서는 연장할 수 없습니다.");
        }
        if (status != LoanStatus.ACTIVE) {
            throw new IllegalStateException("대출 중인 도서만 연장할 수 있습니다.");
        }
        this.dueDate = this.dueDate.plusDays(days);
    }
}

public enum LoanStatus {
    ACTIVE("대출 중"),
    RETURNED("반납 완료"),
    OVERDUE("연체"),
    LOST("분실 처리");

    private final String description;

    LoanStatus(String description) {
        this.description = description;
    }
}
```

**핵심 포인트**:
- ✅ 연체 로직을 도메인 객체에 캡슐화
- ✅ 날짜 계산에 `java.time` API 활용
- ✅ 비즈니스 규칙 검증 포함

---

### 2단계: Repository 패턴

**데이터 접근 계층을 분리하여 비즈니스 로직과 데이터 저장 로직을 독립적으로 관리합니다.**

```java
// 인터페이스 정의
public interface Repository<T, ID> {
    T save(T entity);
    Optional<T> findById(ID id);
    List<T> findAll();
    void delete(ID id);
    boolean existsById(ID id);
}

// Book Repository
public class BookRepository implements Repository<Book, Long> {
    private final Map<Long, Book> books = new LinkedHashMap<>();
    private final AtomicLong idGenerator = new AtomicLong(1);

    @Override
    public Book save(Book book) {
        if (book.getId() == null) {
            book.setId(idGenerator.getAndIncrement());
        }
        books.put(book.getId(), book);
        return book;
    }

    @Override
    public Optional<Book> findById(Long id) {
        return Optional.ofNullable(books.get(id));
    }

    @Override
    public List<Book> findAll() {
        return new ArrayList<>(books.values());
    }

    @Override
    public void delete(Long id) {
        books.remove(id);
    }

    @Override
    public boolean existsById(Long id) {
        return books.containsKey(id);
    }

    // 커스텀 쿼리 메서드
    public List<Book> findByTitle(String title) {
        return books.values().stream()
            .filter(book -> book.getTitle().contains(title))
            .collect(Collectors.toList());
    }

    public List<Book> findByAuthor(String author) {
        return books.values().stream()
            .filter(book -> book.getAuthor().contains(author))
            .collect(Collectors.toList());
    }

    public List<Book> findByCategory(BookCategory category) {
        return books.values().stream()
            .filter(book -> book.getCategory() == category)
            .collect(Collectors.toList());
    }

    public List<Book> findByStatus(BookStatus status) {
        return books.values().stream()
            .filter(book -> book.getStatus() == status)
            .collect(Collectors.toList());
    }

    public Optional<Book> findByIsbn(String isbn) {
        return books.values().stream()
            .filter(book -> book.getIsbn().equals(isbn))
            .findFirst();
    }
}
```

**핵심 포인트**:
- ✅ 제네릭 인터페이스로 공통 메서드 정의
- ✅ Stream API로 유연한 검색 기능 구현
- ✅ 테스트 용이성 (인터페이스 기반)

---

### 3단계: Service 계층

**비즈니스 로직을 조율하고, 여러 Repository를 조합하여 복잡한 작업을 수행합니다.**

```java
public class LibraryService {
    private final BookRepository bookRepository;
    private final MemberRepository memberRepository;
    private final LoanRepository loanRepository;

    public LibraryService(BookRepository bookRepository,
                         MemberRepository memberRepository,
                         LoanRepository loanRepository) {
        this.bookRepository = bookRepository;
        this.memberRepository = memberRepository;
        this.loanRepository = loanRepository;
    }

    // 대출 처리
    public Loan checkoutBook(Long bookId, Long memberId) {
        // 1. 도서 조회
        Book book = bookRepository.findById(bookId)
            .orElseThrow(() -> new IllegalArgumentException("도서를 찾을 수 없습니다."));

        // 2. 회원 조회
        Member member = memberRepository.findById(memberId)
            .orElseThrow(() -> new IllegalArgumentException("회원을 찾을 수 없습니다."));

        // 3. 대출 가능 여부 검증
        if (!book.isAvailable()) {
            throw new IllegalStateException("이 도서는 대출할 수 없습니다.");
        }

        if (!member.canBorrow()) {
            throw new IllegalStateException("대출 한도를 초과했습니다.");
        }

        // 4. 대출 처리
        book.checkout();
        member.incrementLoanCount();

        // 5. 대출 기록 생성
        Loan loan = new Loan();
        loan.setBookId(bookId);
        loan.setMemberId(memberId);
        loan.setLoanDate(LocalDate.now());
        loan.setDueDate(LocalDate.now().plusDays(member.getGrade().getMaxLoanDays()));
        loan.setStatus(LoanStatus.ACTIVE);

        return loanRepository.save(loan);
    }

    // 반납 처리
    public void returnBook(Long loanId) {
        // 1. 대출 기록 조회
        Loan loan = loanRepository.findById(loanId)
            .orElseThrow(() -> new IllegalArgumentException("대출 기록을 찾을 수 없습니다."));

        // 2. 도서 조회
        Book book = bookRepository.findById(loan.getBookId())
            .orElseThrow(() -> new IllegalArgumentException("도서를 찾을 수 없습니다."));

        // 3. 회원 조회
        Member member = memberRepository.findById(loan.getMemberId())
            .orElseThrow(() -> new IllegalArgumentException("회원을 찾을 수 없습니다."));

        // 4. 반납 처리
        loan.returnBook();
        book.returnBook();
        member.decrementLoanCount();

        // 5. 연체료 확인
        if (loan.getOverdueFee() > 0) {
            System.out.println("연체료: " + loan.getOverdueFee() + "원");
        }

        loanRepository.save(loan);
    }

    // 연체 도서 확인
    public List<Loan> getOverdueLoans() {
        return loanRepository.findByStatus(LoanStatus.ACTIVE).stream()
            .filter(Loan::isOverdue)
            .collect(Collectors.toList());
    }

    // 자동 연체 처리 (스케줄러)
    public void processOverdueLoans() {
        List<Loan> overdueLoans = getOverdueLoans();

        for (Loan loan : overdueLoans) {
            loan.setStatus(LoanStatus.OVERDUE);
            loanRepository.save(loan);

            // 회원 정지 (14일 이상 연체)
            if (loan.calculateOverdueDays() >= 14) {
                memberRepository.findById(loan.getMemberId())
                    .ifPresent(Member::suspend);
            }
        }

        System.out.println("연체 처리 완료: " + overdueLoans.size() + "건");
    }
}
```

**핵심 포인트**:
- ✅ 트랜잭션 경계 정의
- ✅ 여러 도메인 객체 조율
- ✅ 비즈니스 규칙 적용

---

## 🎓 기본 실습

### 실습 1: 도서 등록 및 조회

**목표**: 도서를 등록하고 다양한 방법으로 검색하는 기능을 구현합니다.

```java
public class Practice1 {
    public static void main(String[] args) {
        // Repository 생성
        BookRepository bookRepository = new BookRepository();

        // 도서 등록
        Book book1 = new Book();
        book1.setIsbn("978-89-6626-334-4");
        book1.setTitle("이펙티브 자바");
        book1.setAuthor("조슈아 블로크");
        book1.setPublisher("인사이트");
        book1.setCategory(BookCategory.COMPUTER);
        book1.setStatus(BookStatus.AVAILABLE);
        book1.setPublishDate(LocalDate.of(2018, 11, 1));
        bookRepository.save(book1);

        Book book2 = new Book();
        book2.setIsbn("978-89-6626-335-1");
        book2.setTitle("클린 코드");
        book2.setAuthor("로버트 C. 마틴");
        book2.setPublisher("인사이트");
        book2.setCategory(BookCategory.COMPUTER);
        book2.setStatus(BookStatus.AVAILABLE);
        book2.setPublishDate(LocalDate.of(2013, 12, 24));
        bookRepository.save(book2);

        Book book3 = new Book();
        book3.setIsbn("978-89-6626-336-8");
        book3.setTitle("Java의 정석");
        book3.setAuthor("남궁성");
        book3.setPublisher("도우출판");
        book3.setCategory(BookCategory.COMPUTER);
        book3.setStatus(BookStatus.AVAILABLE);
        book3.setPublishDate(LocalDate.of(2016, 1, 27));
        bookRepository.save(book3);

        // 전체 조회
        System.out.println("=== 전체 도서 목록 ===");
        bookRepository.findAll().forEach(book ->
            System.out.printf("[%d] %s - %s%n",
                book.getId(), book.getTitle(), book.getAuthor())
        );

        // 제목으로 검색
        System.out.println("\n=== '자바' 검색 결과 ===");
        bookRepository.findByTitle("자바").forEach(book ->
            System.out.printf("[%d] %s - %s%n",
                book.getId(), book.getTitle(), book.getAuthor())
        );

        // 저자로 검색
        System.out.println("\n=== 저자 '마틴' 검색 결과 ===");
        bookRepository.findByAuthor("마틴").forEach(book ->
            System.out.printf("[%d] %s - %s%n",
                book.getId(), book.getTitle(), book.getAuthor())
        );

        // 카테고리로 검색
        System.out.println("\n=== 컴퓨터 분야 도서 ===");
        bookRepository.findByCategory(BookCategory.COMPUTER).forEach(book ->
            System.out.printf("[%d] %s - %s%n",
                book.getId(), book.getTitle(), book.getAuthor())
        );

        // ISBN으로 검색
        System.out.println("\n=== ISBN 검색 ===");
        bookRepository.findByIsbn("978-89-6626-334-4")
            .ifPresent(book ->
                System.out.printf("찾음: %s - %s%n",
                    book.getTitle(), book.getAuthor())
            );
    }
}
```

**출력 결과**:
```
=== 전체 도서 목록 ===
[1] 이펙티브 자바 - 조슈아 블로크
[2] 클린 코드 - 로버트 C. 마틴
[3] Java의 정석 - 남궁성

=== '자바' 검색 결과 ===
[1] 이펙티브 자바 - 조슈아 블로크
[3] Java의 정석 - 남궁성

=== 저자 '마틴' 검색 결과 ===
[2] 클린 코드 - 로버트 C. 마틴

=== 컴퓨터 분야 도서 ===
[1] 이펙티브 자바 - 조슈아 블로크
[2] 클린 코드 - 로버트 C. 마틴
[3] Java의 정석 - 남궁성

=== ISBN 검색 ===
찾음: 이펙티브 자바 - 조슈아 블로크
```

**학습 포인트**:
- ✅ Repository 패턴의 기본 사용법
- ✅ Stream API를 활용한 검색
- ✅ Optional의 활용

---

### 실습 2: 회원 등록 및 등급 관리

**목표**: 회원을 등록하고 등급별 혜택을 적용합니다.

```java
public class Practice2 {
    public static void main(String[] args) {
        MemberRepository memberRepository = new MemberRepository();

        // 회원 등록
        Member member1 = new Member();
        member1.setMemberNumber("M001");
        member1.setName("홍길동");
        member1.setEmail("hong@example.com");
        member1.setPhone("010-1234-5678");
        member1.setGrade(MemberGrade.BRONZE);
        member1.setStatus(MemberStatus.ACTIVE);
        member1.setRegisteredDate(LocalDate.now());
        member1.setCurrentLoanCount(0);
        memberRepository.save(member1);

        Member member2 = new Member();
        member2.setMemberNumber("M002");
        member2.setName("김영희");
        member2.setEmail("kim@example.com");
        member2.setPhone("010-2345-6789");
        member2.setGrade(MemberGrade.GOLD);
        member2.setStatus(MemberStatus.ACTIVE);
        member2.setRegisteredDate(LocalDate.now());
        member2.setCurrentLoanCount(0);
        memberRepository.save(member2);

        // 회원 정보 출력
        System.out.println("=== 회원 정보 ===");
        memberRepository.findAll().forEach(member -> {
            System.out.printf("회원번호: %s%n", member.getMemberNumber());
            System.out.printf("이름: %s%n", member.getName());
            System.out.printf("등급: %s%n", member.getGrade());
            System.out.printf("최대 대출 권수: %d권%n",
                member.getGrade().getMaxLoanCount());
            System.out.printf("최대 대출 기간: %d일%n",
                member.getGrade().getMaxLoanDays());
            System.out.printf("대출 가능 여부: %s%n",
                member.canBorrow() ? "가능" : "불가능");
            System.out.println();
        });

        // 등급 업그레이드 시뮬레이션
        System.out.println("=== 등급 업그레이드 ===");
        member1.setGrade(MemberGrade.SILVER);
        System.out.printf("%s님의 등급이 %s로 상승했습니다!%n",
            member1.getName(), member1.getGrade());
        System.out.printf("대출 권수: %d권 → %d권%n",
            MemberGrade.BRONZE.getMaxLoanCount(),
            MemberGrade.SILVER.getMaxLoanCount());
        System.out.printf("대출 기간: %d일 → %d일%n",
            MemberGrade.BRONZE.getMaxLoanDays(),
            MemberGrade.SILVER.getMaxLoanDays());
    }
}
```

**출력 결과**:
```
=== 회원 정보 ===
회원번호: M001
이름: 홍길동
등급: BRONZE
최대 대출 권수: 3권
최대 대출 기간: 14일
대출 가능 여부: 가능

회원번호: M002
이름: 김영희
등급: GOLD
최대 대출 권수: 7권
최대 대출 기간: 30일
대출 가능 여부: 가능

=== 등급 업그레이드 ===
홍길동님의 등급이 SILVER로 상승했습니다!
대출 권수: 3권 → 5권
대출 기간: 14일 → 21일
```

**학습 포인트**:
- ✅ Enum에 비즈니스 로직 캡슐화
- ✅ 등급별 정책 적용
- ✅ 상태 관리의 중요성

---

### 실습 3: 대출 및 반납 처리

**목표**: 도서 대출과 반납 프로세스를 구현하고, 연체료를 계산합니다.

```java
public class Practice3 {
    public static void main(String[] args) {
        // Repository 초기화
        BookRepository bookRepository = new BookRepository();
        MemberRepository memberRepository = new MemberRepository();
        LoanRepository loanRepository = new LoanRepository();
        LibraryService libraryService = new LibraryService(
            bookRepository, memberRepository, loanRepository);

        // 테스트 데이터 생성
        Book book = new Book();
        book.setIsbn("978-89-6626-334-4");
        book.setTitle("이펙티브 자바");
        book.setAuthor("조슈아 블로크");
        book.setPublisher("인사이트");
        book.setCategory(BookCategory.COMPUTER);
        book.setStatus(BookStatus.AVAILABLE);
        book.setPublishDate(LocalDate.of(2018, 11, 1));
        book = bookRepository.save(book);

        Member member = new Member();
        member.setMemberNumber("M001");
        member.setName("홍길동");
        member.setEmail("hong@example.com");
        member.setPhone("010-1234-5678");
        member.setGrade(MemberGrade.BRONZE);
        member.setStatus(MemberStatus.ACTIVE);
        member.setRegisteredDate(LocalDate.now());
        member.setCurrentLoanCount(0);
        member = memberRepository.save(member);

        // 대출 처리
        System.out.println("=== 도서 대출 ===");
        System.out.printf("도서: %s%n", book.getTitle());
        System.out.printf("회원: %s (%s)%n", member.getName(), member.getGrade());

        Loan loan = libraryService.checkoutBook(book.getId(), member.getId());

        System.out.printf("대출일: %s%n", loan.getLoanDate());
        System.out.printf("반납 예정일: %s%n", loan.getDueDate());
        System.out.printf("대출 기간: %d일%n",
            member.getGrade().getMaxLoanDays());
        System.out.println("대출이 완료되었습니다.\n");

        // 현재 상태 확인
        System.out.println("=== 현재 상태 ===");
        System.out.printf("도서 상태: %s%n",
            bookRepository.findById(book.getId()).get().getStatus());
        System.out.printf("회원 대출 권수: %d/%d%n",
            memberRepository.findById(member.getId()).get().getCurrentLoanCount(),
            member.getGrade().getMaxLoanCount());
        System.out.println();

        // 연체 시뮬레이션
        System.out.println("=== 연체 시뮬레이션 ===");
        // 반납일을 과거로 설정 (테스트용)
        loan.setDueDate(LocalDate.now().minusDays(5));

        System.out.printf("연체 여부: %s%n",
            loan.isOverdue() ? "연체" : "정상");
        System.out.printf("연체 일수: %d일%n",
            loan.calculateOverdueDays());
        System.out.printf("연체료: %d원%n",
            loan.calculateOverdueFee());
        System.out.println();

        // 반납 처리
        System.out.println("=== 도서 반납 ===");
        libraryService.returnBook(loan.getId());
        System.out.println("반납이 완료되었습니다.");

        // 반납 후 상태
        Loan returnedLoan = loanRepository.findById(loan.getId()).get();
        System.out.printf("반납일: %s%n", returnedLoan.getReturnDate());
        System.out.printf("최종 연체료: %d원%n", returnedLoan.getOverdueFee());
    }
}
```

**출력 결과**:
```
=== 도서 대출 ===
도서: 이펙티브 자바
회원: 홍길동 (BRONZE)
대출일: 2025-10-13
반납 예정일: 2025-10-27
대출 기간: 14일
대출이 완료되었습니다.

=== 현재 상태 ===
도서 상태: CHECKED_OUT
회원 대출 권수: 1/3

=== 연체 시뮬레이션 ===
연체 여부: 연체
연체 일수: 5일
연체료: 500원

=== 도서 반납 ===
연체료: 500원
반납이 완료되었습니다.
반납일: 2025-10-13
최종 연체료: 500원
```

**학습 포인트**:
- ✅ Service 계층의 역할
- ✅ 트랜잭션 처리 (여러 도메인 조율)
- ✅ 날짜 기반 비즈니스 로직
- ✅ 상태 전이 관리

---

## ??FAQ

<details>
<summary><strong>Q1: ?�서관 ?�스?�을 ?�제�?배포?�려�??�떻�??�야 ?�나??</strong></summary>

**A**: 콘솔 ?�플리�??�션?????�는 GUI ?�플리�??�션?�로 ?�환?�고, ?�이?�베?�스�??�결?�야 ?�니??

**?�세 ?�명**:
- ?�인??1: Spring Boot�?REST API 구현
- ?�인??2: MySQL/PostgreSQL ?�이?�베?�스 ?�결
- ?�인??3: React/Vue.js�??�론?�엔??구현
- ?�인??4: Docker�?컨테?�너??

**?�무 ??*:
?�� 먼�? 콘솔 버전?�로 비즈?�스 로직???�성???? 계층??분리?�여 ?�으�??�환?�는 것이 ?�율?�입?�다.
</details>

<details>
<summary><strong>Q2: Repository ?�턴 ?�??직접 Map???�용?�면 ???�나??</strong></summary>

**A**: ?��? ?�로?�트?�서??가?�하지�? ?�장?�과 ?�스???�이?�을 고려?�면 Repository ?�턴???�리?�니??

**?�점 비교**:
```
Repository ?�턴:
???�이???�근 로직 분리
???�스????Mock 객체 ?�용 가??
???�이?�베?�스 ?�환 ??Repository�??�정
??복잡??쿼리�?메서?�로 추상??

직접 Map ?�용:
??Service???�이???�근 로직 분산
???�스???�려?�
???�이?�베?�스 ?�환 ???�체 ?�정
??코드 중복 증�?
```

**?�무 ??*:
?�� 처음부??Repository ?�턴???�용?�면 ?�중??리팩?�링 비용??줄일 ???�습?�다.
</details>

<details>
<summary><strong>Q3: ?�메??객체??setter�??�용?�도 ?�나??</strong></summary>

**A**: 가?�하지�? 불�? 객체(Immutable Object)�??�계?�는 것이 ???�전?�니??

**비교**:
```java
// ??Setter ?�용 (가변)
Book book = new Book();
book.setTitle("?�펙?�브 ?�바");
book.setAuthor("조슈??블로??);
book.setStatus(BookStatus.AVAILABLE);

// ??Builder ?�턴 (불�?)
Book book = Book.builder()
    .title("?�펙?�브 ?�바")
    .author("조슈??블로??)
    .status(BookStatus.AVAILABLE)
    .build();

// ???�성??(불�?)
Book book = new Book(
    "?�펙?�브 ?�바",
    "조슈??블로??,
    BookStatus.AVAILABLE
);
```

**?�무 ??*:
?�� Lombok??`@Builder`�??�용?�면 불�? 객체�??�게 만들 ???�습?�다.
</details>

<details>
<summary><strong>Q4: ?�체�?계산 로직???�디???�어???�나??</strong></summary>

**A**: Loan ?�메??객체???�는 것이 가???�절?�니??(Tell, Don't Ask ?�칙).

**?�못??방법**:
```java
// ??Service??로직
public class LibraryService {
    public int calculateOverdueFee(Loan loan) {
        if (loan.getReturnDate() != null) {
            return 0;
        }
        long days = ChronoUnit.DAYS.between(
            loan.getDueDate(), LocalDate.now());
        return (int) days * 100;
    }
}
```

**?�바�?방법**:
```java
// ???�메??객체??로직
public class Loan {
    public int calculateOverdueFee() {
        int days = calculateOverdueDays();
        return days * 100;
    }

    private int calculateOverdueDays() {
        if (returnDate != null) return 0;
        if (!LocalDate.now().isAfter(dueDate)) return 0;
        return (int) ChronoUnit.DAYS.between(dueDate, LocalDate.now());
    }
}
```

**?�무 ??*:
?�� 비즈?�스 로직?� ?�당 ?�메??객체??캡슐?�하???�집?��? ?�입?�다.
</details>

<details>
<summary><strong>Q5: ?��?중인 ?�서�???��?�려�??�면 ?�떻�?처리?�나??</strong></summary>

**A**: ??�� ?�에 ?��??�태�??�인?�고, ?��?중이�??�외�?발생?�킵?�다.

**구현 ?�시**:
```java
public class BookRepository {
    private final LoanRepository loanRepository;

    public void delete(Long bookId) {
        // 1. ?��?중인지 ?�인
        Book book = findById(bookId)
            .orElseThrow(() -> new IllegalArgumentException("?�서�?찾을 ???�습?�다."));

        if (book.getStatus() == BookStatus.CHECKED_OUT) {
            throw new IllegalStateException("?��?중인 ?�서????��?????�습?�다.");
        }

        // 2. ?��??�력 ?�인 (?�택?�항)
        boolean hasLoanHistory = loanRepository.findAll().stream()
            .anyMatch(loan -> loan.getBookId().equals(bookId));

        if (hasLoanHistory) {
            // ?�프????�� (?�태�?변�?
            book.setStatus(BookStatus.DELETED);
        } else {
            // ?�드 ??��
            books.remove(bookId);
        }
    }
}
```

**?�무 ??*:
?�� ?�무?�서???�부�??�프????��(Soft Delete)�??�용?�여 ?�이???�력??보존?�니??
</details>

<details>
<summary><strong>Q6: ?�원 ?�퇴 ???��??�력?� ?�떻�?처리?�나??</strong></summary>

**A**: ?�원 ?�보�??�명?�하�? ?��??�력?� ?�계 목적?�로 보존?�니??

**구현 ?�시**:
```java
public class MemberService {
    public void withdrawMember(Long memberId) {
        Member member = memberRepository.findById(memberId)
            .orElseThrow(() -> new IllegalArgumentException("?�원??찾을 ???�습?�다."));

        // 1. ?��?중인 ?�서 ?�인
        List<Loan> activeLoans = loanRepository.findByMemberId(memberId).stream()
            .filter(loan -> loan.getStatus() == LoanStatus.ACTIVE)
            .collect(Collectors.toList());

        if (!activeLoans.isEmpty()) {
            throw new IllegalStateException(
                "?��?중인 ?�서가 " + activeLoans.size() + "�??�습?�다. 먼�? 반납?�주?�요.");
        }

        // 2. ?�원 ?�보 ?�명??
        member.setName("?�퇴???�원");
        member.setEmail("withdrawn@example.com");
        member.setPhone("000-0000-0000");
        member.setStatus(MemberStatus.WITHDRAWN);
        member.setWithdrawnDate(LocalDate.now());

        memberRepository.save(member);

        // 3. ?��??�력?� 그�?�??��? (?�계 목적)
    }
}
```

**?�무 ??*:
?�� 개인?�보보호법에 ?�라 개인 ?�별 ?�보???�명?�하?? ?�계 ?�이?�는 보존?�니??
</details>

<details>
<summary><strong>Q7: ?�서�??�약?�는 기능?� ?�떻�?구현?�나??</strong></summary>

**A**: ?�약 Queue�?구현?�고, 반납 ???�음 ?�약?�에�??�동?�로 ?�출합?�다.

**구현 ?�시**:
```java
public class Reservation {
    private Long id;
    private Long bookId;
    private Long memberId;
    private LocalDateTime reservationDate;
    private ReservationStatus status;
    private int queuePosition;
}

public enum ReservationStatus {
    WAITING,    // ?��?�?
    AVAILABLE,  // ?��?가??
    EXPIRED,    // 만료
    CANCELLED   // 취소
}

public class LibraryService {
    // ?�약 추�?
    public Reservation reserveBook(Long bookId, Long memberId) {
        Book book = bookRepository.findById(bookId)
            .orElseThrow(() -> new IllegalArgumentException("?�서�?찾을 ???�습?�다."));

        // ?�재 ?�약 ?��????�인
        List<Reservation> waitingReservations = reservationRepository
            .findByBookIdAndStatus(bookId, ReservationStatus.WAITING);

        Reservation reservation = new Reservation();
        reservation.setBookId(bookId);
        reservation.setMemberId(memberId);
        reservation.setReservationDate(LocalDateTime.now());
        reservation.setStatus(ReservationStatus.WAITING);
        reservation.setQueuePosition(waitingReservations.size() + 1);

        return reservationRepository.save(reservation);
    }

    // 반납 ???�음 ?�약?�에�??�림
    @Override
    public void returnBook(Long loanId) {
        // ... 기존 반납 로직 ...

        // ?�약???�인
        List<Reservation> reservations = reservationRepository
            .findByBookIdAndStatus(book.getId(), ReservationStatus.WAITING)
            .stream()
            .sorted(Comparator.comparing(Reservation::getQueuePosition))
            .collect(Collectors.toList());

        if (!reservations.isEmpty()) {
            Reservation nextReservation = reservations.get(0);
            nextReservation.setStatus(ReservationStatus.AVAILABLE);
            reservationRepository.save(nextReservation);

            // ?�림 발송
            notificationService.sendReservationNotice(
                nextReservation.getMemberId(),
                book.getTitle()
            );
        }
    }
}
```

**?�무 ??*:
?�� ?�약 가???�간???�한?�여 (?? 24?�간) 무한 ?�기�? 방�??�니??
</details>

---

## ?�� 면접 질문 리스??

### 주니??개발??면접 질문 (5-7�?

<details>
<summary><strong>Q1: Repository ?�턴?��? 무엇?�고, ???�용?�나??</strong></summary>

**?��?**:
Repository ?�턴?� ?�이???�근 로직??캡슐?�하??비즈?�스 로직�?분리?�는 ?�계 ?�턴?�니??

**?�용 ?�유**:
1. **관?�사??분리**: ?�이???�??방식??변경되?�도 비즈?�스 로직?� ?�향받�? ?�음
2. **?�스???�이??*: Mock Repository�?만들???�위 ?�스??가??
3. **코드 ?�사??*: 공통 CRUD 로직????곳에 모음
4. **추상??*: ?�이?�베?�스 종류???�립??

**?�시**:
```java
// ?�터?�이?�로 추상??
public interface BookRepository {
    Book save(Book book);
    Optional<Book> findById(Long id);
}

// 메모�?구현
public class InMemoryBookRepository implements BookRepository { ... }

// ?�이?�베?�스 구현
public class JpaBookRepository implements BookRepository { ... }
```

**?�무 ?�인??*:
- Spring Data JPA??Repository ?�턴??구현체입?�다
- ?�터?�이??기반 ?�계�??�장???�보
</details>

<details>
<summary><strong>Q2: ?�메??주도 ?�계(DDD)???�심 개념?� 무엇?��???</strong></summary>

**?��?**:
?�메??주도 ?�계??비즈?�스 ?�메?�을 중심?�로 ?�프?�웨?��? ?�계?�는 방법론입?�다.

**?�심 개념**:
1. **Entity**: 고유 ?�별?��? 가�?객체 (Book, Member)
2. **Value Object**: 값으�?구분?�는 불�? 객체 (Address, Money)
3. **Aggregate**: 관??Entity?�의 묶음 (Order + OrderItem)
4. **Repository**: ?�메??객체???�??조회
5. **Service**: ?�러 ?�메?�을 조율?�는 로직

**?�시 (?�서관 ?�스??**:
```
Entity: Book, Member, Loan
Value Object: ISBN, MemberNumber
Aggregate Root: Member (Member + Loans)
Repository: BookRepository, MemberRepository
Service: LibraryService
```

**?�무 ?�인??*:
- 비즈?�스 로직???�메??객체??캡슐??
- Service???�메??객체�?조율�???
</details>

<details>
<summary><strong>Q3: enum???�용???�태 관리의 ?�점?�?</strong></summary>

**?��?**:
enum?� ?�수 집합???�???�전?�게 관리할 ???�는 Java???�수???�래?�입?�다.

**?�점**:
1. **?�???�전??*: 컴파???�?�에 ?�류 감�?
2. **가?�성**: ?��? ?�는 ?�름 ?�용
3. **?��?보수??*: ?�태 추�?/??��가 ??곳에??관�?
4. **비즈?�스 로직 캡슐??*: enum ?��???메서???�의 가??

**?�시**:
```java
// ??문자???�용 (?�험)
String status = "AVAILABLE";  // ?��? 가??
if (status.equals("AVIALABLE")) { ... }  // ?��????�러

// ??enum ?�용 (?�전)
BookStatus status = BookStatus.AVAILABLE;
if (status == BookStatus.AVAILABLE) { ... }  // 컴파???�??체크

// enum??비즈?�스 로직 추�?
public enum MemberGrade {
    BRONZE(3, 14),
    SILVER(5, 21);

    private final int maxLoanCount;
    private final int maxLoanDays;

    public boolean canBorrow(int currentCount) {
        return currentCount < maxLoanCount;
    }
}
```

**?�무 ?�인??*:
- ?�이?�베?�스?�는 `@Enumerated(EnumType.STRING)`?�로 ?�??
- ?�태 ?�이 규칙??enum???�의 가??
</details>

<details>
<summary><strong>Q4: java.time API�??�용???�짜 계산 방법?�?</strong></summary>

**?��?**:
Java 8부???�입??`java.time` ?�키지??기존 `java.util.Date`??문제?�을 ?�결???��??�인 ?�짜/?�간 API?�니??

**주요 ?�래??*:
```java
// ?�짜�?
LocalDate today = LocalDate.now();
LocalDate dueDate = today.plusDays(14);

// ?�간�?
LocalTime now = LocalTime.now();

// ?�짜 + ?�간
LocalDateTime dateTime = LocalDateTime.now();

// ?�짜 계산
long days = ChronoUnit.DAYS.between(start, end);
long months = ChronoUnit.MONTHS.between(start, end);

// ?�짜 비교
if (today.isAfter(dueDate)) {
    System.out.println("?�체!");
}

// ?�짜 ?�맷
DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-MM-dd");
String formatted = today.format(formatter);
```

**?�무 ?�인??*:
- 불�? 객체?�서 ?�레???�전
- 메서??체이??가??
- `Date`, `Calendar` ?�??`java.time` ?�용
</details>

<details>
<summary><strong>Q5: Optional?� ?�제 ?�용?�야 ?�나??</strong></summary>

**?��?**:
Optional?� 값이 ?�을 ?�도, ?�을 ?�도 ?�는 경우�?명시?�으�??�현?�는 컨테?�너 ?�래?�입?�다.

**?�용 ?�기**:
1. **메서??반환�?*: null??반환??가?�성???�을 ??
2. **?�이??조회**: 조회 결과가 ?�을 ???�을 ??

**?�티?�턴 (?�용?�면 ???�는 경우)**:
```java
// ???�드�??�용
public class Member {
    private Optional<String> email;  // X
}

// ??메서???�라미터�??�용
public void updateMember(Optional<String> name) { ... }  // X

// ??컬렉?�을 Optional�?감싸�?
public Optional<List<Book>> findBooks() { ... }  // X
// �?리스??반환?????�절
public List<Book> findBooks() { return Collections.emptyList(); }
```

**?�바�??�용**:
```java
// ??조회 결과
public Optional<Book> findById(Long id) {
    return Optional.ofNullable(books.get(id));
}

// ???�전???�용
bookRepository.findById(id)
    .ifPresent(book -> System.out.println(book.getTitle()));

Book book = bookRepository.findById(id)
    .orElseThrow(() -> new IllegalArgumentException("?�서�?찾을 ???�습?�다."));
```

**?�무 ?�인??*:
- `get()` 직접 ?�출 금�? ??`orElseThrow()` ?�용
- `isPresent()` + `get()` 조합 금�? ??`ifPresent()` ?�용
</details>

<details>
<summary><strong>Q6: ?�랜??��?��? 무엇?�고, ???�요?��???</strong></summary>

**?��?**:
?�랜??��?� ?�이?�베?�스???�태�?변경하???�나???�리???�업 ?�위?�니??

**ACID ?�성**:
1. **Atomicity (?�자??**: All or Nothing (?��? ?�공 ?�는 ?��? ?�패)
2. **Consistency (?��???**: ?�이??무결???��?
3. **Isolation (격리??**: ?�시 ?�행 중인 ?�랜??�� �??�립??
4. **Durability (지?�성)**: ?�료???�랜??��?� ?�구 ?�??

**?�서관 ?�스???�시**:
```java
// ?��?처리 = ?�나???�랜??��
public Loan checkoutBook(Long bookId, Long memberId) {
    // 1. ?�서 ?�태 변�?
    book.checkout();  // CHECKED_OUT?�로 변�?

    // 2. ?�원 ?��?권수 증�?
    member.incrementLoanCount();

    // 3. ?��?기록 ?�성
    Loan loan = new Loan();
    loanRepository.save(loan);

    // 만약 중간???�러 발생?�면?
    // ??모든 변경사??롤백
    // ???�서 ?�태, ?�원 권수 모두 ?�상복구
}
```

**Spring?�서???�랜??��**:
```java
@Transactional
public Loan checkoutBook(Long bookId, Long memberId) {
    // 메서???�체가 ?�나???�랜??��
    // ?�러 발생 ???�동 롤백
}
```

**?�무 ?�인??*:
- `@Transactional`?� public 메서?�에�??�용
- checked exception?� 기본?�으�?롤백 ????
- `@Transactional(rollbackFor = Exception.class)` ?�정 권장
</details>

<details>
<summary><strong>Q7: 계층???�키?�처??�?계층 ??��?�?</strong></summary>

**?��?**:
계층???�키?�처??관?�사�?분리?�여 �?계층??명확??책임??가지?�록 ?�계?�는 방식?�니??

**3-Tier ?�키?�처**:
```
?��??�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�??
?? Presentation Layer  ?? ??UI, Controller
?? (?�현 계층)          ??
?��??�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�??
??  Business Layer     ?? ??Service, Domain Logic
?? (비즈?�스 계층)      ??
?��??�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�??
?? Persistence Layer   ?? ??Repository, Database
?? (?�속??계층)        ??
?��??�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�??
```

**�?계층????��**:
```java
// 1. Presentation Layer (Controller)
public class LibraryController {
    public void handleCheckout() {
        // ?�용???�력 받기
        // Service ?�출
        // 결과 출력
    }
}

// 2. Business Layer (Service)
public class LibraryService {
    public Loan checkoutBook(Long bookId, Long memberId) {
        // 비즈?�스 로직 조율
        // ?�러 Repository 조합
        // ?�랜??�� 관�?
    }
}

// 3. Persistence Layer (Repository)
public class BookRepository {
    public Optional<Book> findById(Long id) {
        // ?�이???�??조회
        // SQL ?�행
    }
}
```

**계층 분리???�점**:
- ??관?�사 분리�?코드 ?�해 ?��?
- ??�?계층 ?�립?�으�??�스??가??
- ????계층??변경이 ?�른 계층???�향 ?�음
- ???�사?�성 ?�상

**?�무 ?�인??*:
- ?�위 계층�??�존 (Controller ??Service ??Repository)
- ?�환 ?�존 금�?
</details>

---

### 중급 개발??면접 질문 (3-5�?

<details>
<summary><strong>Q1: ?�메???�벤??Domain Event)?� 무엇?�고, ?�떻�??�용?�나??</strong></summary>

**?��?**:
?�메???�벤?�는 ?�메???�에??발생??중요???�건???��??�는 객체?�니??

**?�용 ?�시 (?�서관 ?�스??**:
```java
// ?�벤???�의
public class BookCheckedOutEvent {
    private final Long bookId;
    private final Long memberId;
    private final LocalDateTime occurredAt;

    public BookCheckedOutEvent(Long bookId, Long memberId) {
        this.bookId = bookId;
        this.memberId = memberId;
        this.occurredAt = LocalDateTime.now();
    }
}

// ?�벤??발행
public class LibraryService {
    private final ApplicationEventPublisher eventPublisher;

    public Loan checkoutBook(Long bookId, Long memberId) {
        // ?��?처리
        Loan loan = ...;

        // ?�벤??발행
        eventPublisher.publishEvent(
            new BookCheckedOutEvent(bookId, memberId));

        return loan;
    }
}

// ?�벤??리스??
@Component
public class BookCheckedOutEventListener {
    @EventListener
    public void handleBookCheckedOut(BookCheckedOutEvent event) {
        // 1. ?�림 발송
        notificationService.sendCheckoutNotice(event.getMemberId());

        // 2. ?�계 ?�데?�트
        statisticsService.incrementCheckoutCount(event.getBookId());

        // 3. ?�약 ?�기자?�게 ?�림
        reservationService.notifyNextInQueue(event.getBookId());
    }
}
```

**?�점**:
- ???�슨??결합: Service?� 부가 기능 분리
- ???�장?? ?�로??리스??추�?가 ?��?
- ???�일 책임 ?�칙: �?리스?��? ??가지�?처리

**?�무 ?�인??*:
- Spring??`@EventListener` ?�는 Kafka/RabbitMQ ?�용
- ?�벤?�는 불�? 객체�??�계
- ?�벤???�?�소(Event Store)�??�력 관�?가??
</details>

<details>
<summary><strong>Q2: ?�시??문제???�떻�??�결?�나??</strong></summary>

**?��?**:
?�러 ?�용?��? ?�시??같�? ?�서�??�출하??????발생?�는 문제�??�시??문제?�고 ?�니??

**문제 ?�나리오**:
```
?�간 | ?�용?�A              | ?�용?�B
------|---------------------|-------------------
T1    | ?�서 조회 (?��?가?? |
T2    |                     | ?�서 조회 (?��?가??
T3    | ?��?처리            |
T4    |                     | ?��?처리 (중복!)
```

**?�결 방법 1: ?��?????Optimistic Lock)**:
```java
@Entity
public class Book {
    @Id
    private Long id;

    @Version  // 버전 관�?
    private Long version;

    private BookStatus status;
}

// ?�용
try {
    book.checkout();
    bookRepository.save(book);
} catch (OptimisticLockException e) {
    throw new ConcurrentModificationException(
        "?�른 ?�용?��? 먼�? ?�출했?�니??");
}
```

**?�결 방법 2: 비�?????Pessimistic Lock)**:
```java
@Lock(LockModeType.PESSIMISTIC_WRITE)
Optional<Book> findByIdForUpdate(Long id);

// ?�용
Book book = bookRepository.findByIdForUpdate(id)
    .orElseThrow(...);
book.checkout();
```

**?�결 방법 3: 분산 ??Distributed Lock)**:
```java
@Component
public class RedisLockService {
    public <T> T executeWithLock(String lockKey, Supplier<T> action) {
        String lock = redisTemplate.opsForValue()
            .setIfAbsent(lockKey, "locked", 10, TimeUnit.SECONDS);

        if (lock) {
            try {
                return action.get();
            } finally {
                redisTemplate.delete(lockKey);
            }
        } else {
            throw new ConcurrentModificationException("???�득 ?�패");
        }
    }
}
```

**?�무 ?�택 기�?**:
- ?�기 많음 ???��?????
- ?�기 많음 ??비�?????
- 분산 ?�스????Redis/Zookeeper 분산 ??
</details>

<details>
<summary><strong>Q3: CQRS ?�턴???�서관 ?�스?�에 ?�떻�??�용?�나??</strong></summary>

**?��?**:
CQRS(Command Query Responsibility Segregation)???�기?� ?�기 모델??분리?�는 ?�턴?�니??

**기본 구조**:
```java
// Command (?�기) - ?�이??변�?
public interface CommandService {
    Loan checkoutBook(Long bookId, Long memberId);
    void returnBook(Long loanId);
}

// Query (?�기) - ?�이??조회
public interface QueryService {
    BookDTO findBook(Long id);
    List<BookDTO> searchBooks(String keyword);
    LoanStatisticsDTO getStatistics();
}

// Command Model (?�규??
@Entity
public class Book {
    @Id
    private Long id;
    private String title;
    private String author;
    // ... ?�규?�된 구조
}

// Query Model (비정규화, 조회 최적??
@Entity
public class BookView {
    @Id
    private Long id;
    private String title;
    private String author;
    private String categoryName;      // 조인 결과 ?�함
    private int totalLoanCount;       // 집계 결과 ?�함
    private LocalDateTime lastUpdated;
}
```

**?�벤???�싱�??�께 ?�용**:
```java
// 1. Command 처리 ???�벤???�??
public Loan checkoutBook(Long bookId, Long memberId) {
    Loan loan = ...;

    // ?�벤??발행
    eventStore.save(new BookCheckedOutEvent(...));

    return loan;
}

// 2. Query 모델 ?�데?�트
@EventListener
public void updateBookView(BookCheckedOutEvent event) {
    BookView view = bookViewRepository.findById(event.getBookId());
    view.setTotalLoanCount(view.getTotalLoanCount() + 1);
    view.setLastUpdated(LocalDateTime.now());
    bookViewRepository.save(view);
}
```

**?�점**:
- ???�기 ?�능 최적??(?�덱?? 캐시 ?�략 ?�립??
- ??복잡??쿼리�?미리 계산?�여 ?�??
- ???�기/?�기 부?�에 ?�라 ?�립?�으�??��??�링

**?�무 ?�용**:
- ?�규모 ?�스?�에???�과??
- ?�규모에?�는 ?�버?��??�어�??�험
</details>

<details>
<summary><strong>Q4: 마이?�로?�비???�키?�처�??�환 ??고려?�항?�?</strong></summary>

**?��?**:
?�서관 ?�스?�을 마이?�로?�비?�로 분리?????�음 ?�항??고려?�야 ?�니??

**?�비??분리 ?�략**:
```
?��??�?�?�?�?�?�?�?�?�?�?�?�?? ?��??�?�?�?�?�?�?�?�?�?�?�?�?�?? ?��??�?�?�?�?�?�?�?�?�?�?�??
??Book Service?? ?�Member Service?? ?�Loan Service??
??            ?? ??             ?? ??           ??
??- ?�서 관�? ?? ??- ?�원 관�?  ?? ??- ?��?관�???
??- ?�서 검?? ?? ??- ?�급 관�?  ?? ??- 반납 관�???
?��??�?�?�?�?�?�?�?�?�?�?�?�?? ?��??�?�?�?�?�?�?�?�?�?�?�?�?�?? ?��??�?�?�?�?�?�?�?�?�?�?�??
       ??               ??                 ??
       ?��??�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?��??�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�?�??
                        ??
                  ?��??�?�?�?�?�?�?�?�?�??
                  ??API      ??
                  ??Gateway  ??
                  ?��??�?�?�?�?�?�?�?�?�??
```

**?�이???��???문제**:
```java
// 문제: 분산 ?�랜??��
// Book Service?� Loan Service가 분리?�어 ?�음

// ?�결 1: Saga ?�턴
public class CheckoutSaga {
    public void execute(Long bookId, Long memberId) {
        try {
            // 1. Book Service: ?�서 ?�태 변�?
            bookService.reserve(bookId);

            // 2. Member Service: ?��?권수 증�?
            memberService.incrementLoanCount(memberId);

            // 3. Loan Service: ?��?기록 ?�성
            loanService.createLoan(bookId, memberId);

        } catch (Exception e) {
            // 보상 ?�랜??�� ?�행
            bookService.unreserve(bookId);
            memberService.decrementLoanCount(memberId);
            throw e;
        }
    }
}

// ?�결 2: ?�벤??기반 ?��???
// 1. Loan Service가 BookCheckedOutEvent 발행
// 2. Book Service가 ?�벤???�신?�여 ?�태 변�?
// 3. 최종 ?��???Eventual Consistency) ?�성
```

**?�비??�??�신**:
```java
// ?�기 ?�신 (REST API)
@FeignClient(name = "book-service")
public interface BookServiceClient {
    @GetMapping("/books/{id}")
    BookDTO getBook(@PathVariable Long id);
}

// 비동�??�신 (Message Queue)
@Service
public class LoanEventPublisher {
    @Autowired
    private RabbitTemplate rabbitTemplate;

    public void publishCheckoutEvent(Loan loan) {
        rabbitTemplate.convertAndSend(
            "loan-exchange",
            "checkout",
            new BookCheckedOutEvent(loan)
        );
    }
}
```

**?�무 고려?�항**:
- ???�비??경계�??�메??중심?�로 ?�정
- ??�??�비?�는 ?�립?�인 ?�이?�베?�스 ?�유
- ??API Gateway�??�합 ?�드?�인???�공
- ??분산 추적(Distributed Tracing) ?�입 ?�수
</details>

---

## ?�� ?�심 ?�리

### ?�서관 ?�스???�심 ?�소

```
1. ?�메??모델
   ?��? Book (?�서)
   ?��? Member (?�원)
   ?��? Loan (?��?

2. ?�계 ?�턴
   ?��? Repository ?�턴
   ?��? Service 계층
   ?��? enum ?�태 관�?

3. 비즈?�스 로직
   ?��? ?��?가???��? 검�?
   ?��? ?�체�?계산
   ?��? ?�동 ?�체 처리

4. ?�심 기술
   ?��? Optional
   ?��? Stream API
   ?��? java.time API
```

### ?�습???�용

- ???�메??주도 ?�계(DDD)??기본 개념
- ??계층???�키?�처???�계 ?�칙
- ??Repository ?�턴???�점
- ??enum???�용???�???�전???�태 관�?
- ??Optional???�용??null ?�전??
- ??java.time API�??�용???�짜 계산
- ??비즈?�스 로직??캡슐??
- ??Service 계층????���?책임

---

## ?? ?�음 ?�계

### ?�장 기능

```
1. ?�약 ?�스??
   - ?��?중인 ?�서 ?�약
   - ?�약 ?�기열 관�?

2. ?�림 ?�스??
   - 반납??3?????�림
   - ?�약 ?�서 ?��?가???�림

3. ?�계 �?리포??
   - ?�기 ?�서 ?�위
   - ?�원�??��??�계

4. 추천 ?�스??
   - ?�업 ?�터�?기반 추천
   - ?��??�력 기반 추천
```

### 기술 ?�택 ?�그?�이??

```bash
# Spring Boot�??�환
spring.io/projects/spring-boot

# ?�이?�베?�스 ?�동
- H2 Database (개발)
- MySQL/PostgreSQL (?�영)

# ???�터?�이??
- Thymeleaf (?�버 ?�이??
- React/Vue.js (SPA)

# 배포
- Docker 컨테?�너??
- AWS/GCP ?�라?�드 배포
```

### 관??기술

```
1. Spring Data JPA
   - Repository 구현 ?�동??
   - 복잡??쿼리 ?�성

2. Spring Security
   - ?�증/?��?
   - Role 기반 ?�근 ?�어

3. Redis
   - 캐싱
   - ?�션 관�?

4. Kafka
   - ?�벤???�트리밍
   - 마이?�로?�비???�신
```

---

## ?�� 축하?�니??

**?�제 ?�러분�?**:
- ???�무 ?��????�메??모델???�계?????�습?�다
- ??Repository ?�턴�?Service 계층???�해?�고 ?�용?????�습?�다
- ??객체지???�계 ?�칙???�제 ?�로?�트???�용?????�습?�다
- ??복잡??비즈?�스 로직??체계?�으�?관리할 ???�습?�다

**?�음 ?�계**:
- [ ] Spring Boot�????�플리�??�션 ?�환
- [ ] JPA�??�이?�베?�스 ?�동
- [ ] React�??�론?�엔??구현
- [ ] 51?�으�??�동: Java 베스???�랙?�스

---

**?�음 ?�으�??�동**: [?�음: 51??Java 베스???�랙?�스](51-Java-베스???�랙?�스.md)

**?�체 목차�??�아가�?*: [Java ?�체 목차](README.md)
